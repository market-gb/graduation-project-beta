server:
  port: 8000
  servlet:
    context-path: /beta-version
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/beta
logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.hibernate: INFO